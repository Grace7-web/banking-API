# render.yaml
# Configuration de déploiement sur Render.com

services:
  - type: web
    name: banking-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
